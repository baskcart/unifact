# Server-only: Postgres origin (omit for local SQLite)
# DATABASE_URL=postgresql://user:ENCODED_PASSWORD@HOST:5432/unifact?sslmode=require

# Optional listen port
PORT=4110

# Auth: one API key per person in the api_keys table (uni key create / on / off).
# No UNIFACT_MASTER_KEY. No UNIFACT_API_KEY env.
