# UniFact — Claude / Cowork

Use **UniFact MCP** for organizational truth (see `docs/mcp.md`). Before work that depends on how this org operates: `sync_pull`, then search/list facts. Do not invent policy from the README when facts exist.

Host discovery (no key): `GET /.well-known/unifact.json` — [`docs/frameworks.md`](docs/frameworks.md). Config vs secrets: [`docs/config-as-facts.md`](docs/config-as-facts.md).
